import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.realitysprinttracker.app',
  appName: '35LPA Reality Sprint Tracker',
  webDir: 'dist/reality-sprint-tracker/browser'
};

export default config;
